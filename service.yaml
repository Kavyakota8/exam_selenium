apiVersion: v1
kind: Service
metadata:
  name: formapp-service
spec:
  selector:
    app: formapp
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30188
  type: NodePort